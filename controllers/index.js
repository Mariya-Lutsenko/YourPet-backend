const auth = require("./auth");
const {
  addNoticesToCategory,
  getNoticesById,
  getNotices,
} = require("./notices");

module.exports = {
  auth,
  addNoticesToCategory,
  getNoticesById,
  getNotices,
};
